# ===== VITE_API_TOKEN：前端构建期令牌（轮换需重建前端镜像）=====
# 构建时通过 compose build.args.VITE_API_TOKEN 传入 Dockerfile stage1，烘焙进前端静态 bundle。
# 运行时由浏览器随每个 /api 请求以 Authorization: Bearer 方式携带，与服务端 TOKEN 做恒等比较。
# 注意：此值会被打包进 JS，浏览器端可见；发布到公网必须配 HTTPS。
# 轮换：改此变量 + TOKEN 后必须重建镜像（docker compose up -d --build）才会生效。
VITE_API_TOKEN=change-me-to-one-long-random-string

# ===== TOKEN：服务端共享口令（运行时鉴权，必填）=====
# server/server.js 对所有 /api 请求做 Bearer 鉴权，不匹配返回 401。
# 必填：缺失时 compose 的 ${TOKEN:?} 强校验直接报错；即使绕过 compose，容器内服务进程也会 fail-fast 退出。
# 与 VITE_API_TOKEN 一样，都使用【同一个】共享口令，生产请设置为足够长的随机串。
TOKEN=change-me-to-one-long-random-string
